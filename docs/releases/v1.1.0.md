## Summary

- improve onboarding output flow with a dedicated output-choice step
- clipboard-first output is now the default; auto-paste is treated as an advanced option
- saved history is opt-in for privacy
- aggregate usage stats store counts and durations only, never transcribed text
- release safety improvements for the packaging and signing pipeline

## What's Changed

- added a dedicated output configuration step to onboarding (clipboard vs. auto-paste)
- changed default output behavior: clipboard copy on, auto-paste off, history off
- added privacy-safe aggregate usage stats (word count, duration, session count — no text)
- added usage stats header in Settings
- improved release packaging script with signing and notarization support
- added Homebrew cask update automation via `update_homebrew_cask.py`
