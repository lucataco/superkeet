## Summary
- fix the release pipeline so manual publishes build the requested tag instead of the current `main` checkout
- fall back to ad-hoc signing cleanly when Apple certificate secrets are incomplete, instead of failing mid-package
- keep the automated GitHub release and Homebrew tap update flow in sync for future tagged releases
