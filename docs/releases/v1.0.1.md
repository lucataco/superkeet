## Summary
- fix onboarding to defer the accessibility prompt until the Permissions step instead of triggering it on launch
- ignore release verification downloads so they don't pollute the repo
- fix release workflow validation for tag/version matching
